<html>
  <head>
    <title>Doctors Office</title>
  </head>
<body>
    <h1>Welcome to Your Doctor's Office</h1>

    <p>Find your doctor:</p>

    <form action="/doctors" method="post">
        <label for="name">Doctor name:</label>
        <input id="name" name="name" type="text">
        <label for="specialty">Doctor specialty:</label>
        <input id="specialty" name="specialty" type="text">
        <button type="submit">Find doctor</button>
    </form>

    {% if doctors is not empty %}
        <p>Here are all your doctors:</p>
        <ul>
            {% for doctor in doctors %}
                <li><a href="/doctors/{{ doctor.getId }}">{{ doctor.getName }} : {{ doctor.getSpecialty }}</a></li>
            {% endfor %}
        </ul>
    {% else %}
      <p>There are no doctors yet!</p>
    {% endif %}

</body>
</html>
